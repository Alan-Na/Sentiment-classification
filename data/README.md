## Data placement

Put your cleaned dataset here:

- `data/training_data_clean.csv`

The CSV must contain:
- `label` (target class as string)
- `student_id` (optional, used for group-aware split; if missing, row index is used)

⚠️ Do **NOT** commit the dataset to GitHub.
